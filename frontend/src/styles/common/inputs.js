import styled from 'styled-components';
import {
    TextField
} from '@material-ui/core';

export const InputOutlined = styled(TextField).attrs(props => ({
    variant: "outlined"
}))`
    margin: 1rem;
`

export const InputStd = styled(TextField)`
    
`